apiVersion: apps/v1
kind: Deployment
metadata:
  name: translation
spec:
  selector:
    matchLabels:
      app: translation
  replicas: 2
  template:
    metadata:
      labels:
        app: translation
    spec:
      containers:
        - name: dev-translation
          image: petrovtoncho/translation
          ports:
            - name: t-port
              containerPort: 8082
          env:
            - name: RABBITMQ_HOSTNAME
              value: rabbit-service